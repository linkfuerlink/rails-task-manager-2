<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <% unless f.object.new_record? %>
    <%= f.input :completed %>
  <% end %>
  <%= f.submit class: 'btn btn-primary'%>
<% end %>
